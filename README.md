# Just a Few Icons

A static GitHub Pages gallery for a small SVG icon set.

There is no build step. Edit the SVG files in `icons/`, update the icon list in `icons.js`, and open `index.html` through a local static server or GitHub Pages.

Preview locally:

```sh
npx serve .
```
